#include <stdio.h>
#include "utils.h"

/**
 * @brief Multiply two floats and prints the result
 * @param a One float
 * @param b The other float
 */
void multiplyAndPrint(const float a, const float b) {
    printf("%f times %f eqauls %f\n", a, b, a * b);
}
