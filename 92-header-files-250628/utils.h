// Header guards
// They only define the function if not already defined somewhere else
// This is done by creating a macro (UTILS.H) and checking for its presence
#ifndef UTILS_H
#define UTILS_H 

int mystrlen(const char * inputString, const int max);

#endif
